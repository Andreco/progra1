#include "stdafx.h"
#include <iostream>
#include "conio.h"
using namespace std; 
bool num_par (int a); 
void main ()
{
	bool z;
	int numero; 
	cout<<"ingrese numero"<<endl; 
	cin>>numero; 
 z=num_par (numero); 
	 if  
		 (z)
		 cout<<"es par"<<endl;
	 else 
		 cout<<"es impar"<<endl;
	getch();
} 
bool num_par (int a)
{ if (a%2==0) 
return true; 
else 
return false;

}
